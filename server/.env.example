# Server
PORT=3001
HOST=0.0.0.0

# CORS — comma-separated list of allowed origins
# For development: chrome-extension://YOUR_EXTENSION_ID
# For production: add your deployed extension's origin
CORS_ORIGINS=*

# Database path (relative to server root)
DB_PATH=./data/notes.db
